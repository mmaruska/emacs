** M-x report-emacs-bug CCs the report to the maintainer.
   Provided by: Romain Francoise <rfrancoise@debian.org>

---
 lisp/mail/emacsbug.el |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

--- a/lisp/mail/emacsbug.el
+++ b/lisp/mail/emacsbug.el
@@ -106,7 +106,7 @@
     (setq message-end-point
 	  (with-current-buffer (get-buffer-create "*Messages*")
 	    (point-max-marker)))
-    (compose-mail reporting-address topic)
+    (compose-mail reporting-address topic '(("Cc" . "rfrancoise@debian.org")))
     ;; The rest of this does not execute if the user was asked to
     ;; confirm and said no.
     ;; Message-mode sorts the headers before sending.  We sort now so
